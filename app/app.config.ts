export default defineAppConfig({
  ui: {
    // Semantic color configuration
    colors: {
      primary: 'blue', // Using blue for primary (matches --colour-primary: #60a5fa)
      secondary: 'cyan', // Using cyan for secondary/gradient end (#5eead4)
      neutral: 'slate', // Using slate for neutral tones
    },
  },
})
